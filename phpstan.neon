# Warning: It will cause error if update to larastan 2.10 because of it change phpmyadmin/sql-parser (5.10.3) to iamcal/sql-parser (v0.5)
includes:
    - vendor/larastan/larastan/extension.neon

parameters:

    paths:
        - app/
    
    excludePaths:
        - tests/

    # Level 9 is the highest level
    level: 5
